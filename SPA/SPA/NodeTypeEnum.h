#ifndef NodeTypeEnum_H
#define NodeTypeEnum_H

enum NodeType {
	program, procedure, statementLst, assign, call, whilestmt, ifstmt,
	variable, constant, plus, times, minus, divide, default, elf
};

#endif